---
export interface Props{
	nombre: string;
	ocupacion?: string;
	avatar:boolean;
}
const { nombre, ocupacion = "Sin definir", avatar } = Astro.props

---
<script>
	const button = document.getElementById('btn');
	button?.addEventListener("click", function(){
		alert('hola')
	})
</script>
<button id="btn">Click</button>
<div class="card">
	<img src={avatar ? "images/avatar1.jpg" : "images/avatar2.jpg"} alt="imagen" style="width: 100%;" />
	<div class="container">
		<h4>{nombre}</h4>
		<p>{ocupacion}</p>
		<slot name="desc"/>
		<p>Contenido de la descripción</p>
	</slot>
	</div>
</div>

<style>
	.card{
		box-shadow: 0 4px 8px 0 rgba(0, 0, 0, 0.2);
		transition: 0.3s;
		margin-left: 50px;
		width: 15%;
		margin-bottom: 15px;
		padding: 20px;
	}
	.card:hover{
		box-shadow: 0 8px 16px 0 rgba(0, 0, 0, 0.2);
	}
	.container{
		padding: 2px, 16px:
	}
</style>